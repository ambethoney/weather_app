import React from 'react';
import { Link } from 'react-router';

const Single = React.createClass({

  render() {
    // Then we go ahead and return some JSX
    return (
      <p>hello from single!</p>
    );
  }

});

export default Single;
